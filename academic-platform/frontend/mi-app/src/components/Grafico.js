const Grafico = () => {
  const chartRef = useRef(null);

  useEffect(() => {
    // tu lógica aquí
  }, []);

  return (
    <div ref={chartRef}>
      {/* tu gráfico aquí */}
    </div>
  );
};

export default Grafico;
